# Frontend API Contract

The Vue UI is intentionally not implemented yet. This folder only keeps API client modules for the future Vue 3 + Vite + Element Plus frontend.

Expected backend base URL:

```env
VITE_API_BASE_URL=http://localhost:8000/api
```
